image: node:lts

pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp -r css/ public/
    - cp -r js/ public/
    - cp -r imagenes/ public/
    - cp VideoSitioWeb_OPT.mp4 public/
  artifacts:
    paths:
      - public
  only:
    - main
